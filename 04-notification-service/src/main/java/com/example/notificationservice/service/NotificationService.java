package com.example.notificationservice.service;

// empty scaffold left over from initial project setup, NotificationProviderService ended up
// being the class that actually sends emails, nothing wired to this one
