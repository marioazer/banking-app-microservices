package com.example.notificationservice.controller;

// empty scaffold, this service never actually needed a public rest api, everything here is
// driven by kafka listeners (TransactionAlertListener/ProfileNotificationListener) and a
// scheduled job instead, nothing wired to this class
