package com.example.notificationservice.model;

// empty scaffold, this service never actually persists notifications to its own database,
// it just reacts to kafka events and calls out to an email provider in real time
